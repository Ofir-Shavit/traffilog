import path from 'path';

export default {
    workerPoolSize: 4,
    inputCsvPath: path.join(__dirname, '../coordinates_for_node_test (1).csv'),
    outputCsvPath: path.join(__dirname, '../coordinates_for_node_test (1)_output.csv')
};